# Solo documentación local — NO commitear .env con valores reales.
# En Vercel: Project → Settings → Environment Variables (no uses archivos .env en el repo).

# Variable que usa el build de producción (scripts/set-env.js):
# API_URL=https://tu-servicio.onrender.com/api
